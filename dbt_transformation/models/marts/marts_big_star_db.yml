version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not null
      - name: email
        tests:
          - not null
      - name: city
        tests:
          - not null
      - name: country
        tests:
          - not null